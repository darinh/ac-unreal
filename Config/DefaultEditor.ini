; =====================================================================
; DefaultEditor.ini — editor-only defaults. Doesn't affect Game target.
; =====================================================================

[/Script/UnrealEd.UnrealEdEngine]
TexturePropertiesUseRGB=True

[ContentBrowser]
ShowEngineContent=False
ShowOnlyAssetsInSelectedFolders=False
ShowPluginContent=False

[ProjectBrowser]
; Open the project at the C++ Classes view so the user immediately
; sees our Phase 2 UCharacterMovementComponent / UDataAsset /
; UWorldSubsystem classes.
